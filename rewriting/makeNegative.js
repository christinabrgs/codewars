function makeNegative (num) {
    if (num === 0 || num < 0) {
        return num
    }
    else { return num * -1 }
}

function makeNegative(number) {
    return number < 0 ? number : -number;
}

function makeNegative(number) {
    return number <= 0 ? number : -number
}

function makeNegative (number) {
    return number <= 0 ? number : -number
}

function makeNegative (number) {
    return number <= 0 ? number : -number
}

function makeNegative (number) {
    return number <= 0 ? number : -number
}

function makeNegative (number) {
    return number <= 0 ? number : -number
}

function makeNegative (number) {
    return number <= 0 ? number : -number
}

function makeNegative (number) {
    return number <= 0 ? number : -number
}

function makeNegative (number) {
    return number <= 0 ? number : -number
}

function makeNegative (number) {
    return number <= 0 ? number : -number
}